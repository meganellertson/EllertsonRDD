McCrary Density 

libary(rddensity)
rdbwdensity(
  bac1,
  c = 0.8 & 0.15,
  p = 2,
  fitselect = "",
  kernel = "",
  vce = "",
  masPoint = TRUE,
  regularize = TRUE,
  nLocalMin = NULL,
  nUniqueMin = NULL
  
)